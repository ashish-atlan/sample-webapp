# Comma-separated list of origins allowed to postMessage into the embedded app.
# In dev, http://localhost:5173 and http://127.0.0.1:5173 are also accepted automatically.
# In production, set this to your Atlan tenant origin(s):
VITE_ATLAN_ALLOWED_ORIGINS=https://your-tenant.atlan.com
